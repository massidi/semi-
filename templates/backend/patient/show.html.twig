{% extends 'admin.html.twig' %}

{% block title %}Patient{% endblock %}

{% block contenu %}
    <h1>Patient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patient.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ patient.type }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ patient.age }}</td>
            </tr>
            <tr>
                <th>Weigth</th>
                <td>{{ patient.weigth }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ patient.address }}</td>
            </tr>
            <tr>
                <th>Mobile</th>
                <td>{{ patient.mobile }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ patient.image }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ patient.updatedAt ? patient.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('patient_index') }}">back to list</a>

    <a href="{{ path('patient_edit', {'id': patient.id}) }}">edit</a>

    {{ include('backend/patient/_delete_form.html.twig') }}
{% endblock %}
