{#<form method="post" action="{{ path('doctor_delete', {'id': medic_prescription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#    <input type="hidden" name="_method" value="DELETE">#}
{#    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medic_prescription.id) }}">#}

{#</form>#}
<form method="post" action="{{ path('prescription_delete', {'id': prescriptions.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescriptions.id) }}">
    <button class="btn btn-danger">Delete</button>

</form>
