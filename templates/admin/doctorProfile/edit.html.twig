
{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('admin/doctorProfile/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_index') }}">back to list</a>

    {{ include('admin/doctorProfile/_delete_form.html.twig') }}
{% endblock %}
{#{{ dump(doctorProfile) }}#}